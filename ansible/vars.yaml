---

ntp-server:
- 0.fr.pool.ntp.org
- 1.fr.pool.ntp.org

# ssh_port: 7022

# Variables globales pour Prometheus
prometheus_version: "2.47.0"
prometheus_config_flags:
  web.listen-address: "0.0.0.0:9090"

# Variables globales pour Grafana
grafana_version: "10.2.3"
grafana_admin_password: "monmotdepasse"
